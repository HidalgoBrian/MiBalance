services:
  - type: web
    name: mibalance
    runtime: node
    plan: free
    buildCommand: npm install && npm install --save-dev @types/node && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
