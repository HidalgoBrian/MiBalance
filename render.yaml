services:
  - type: web
    name: mibalance
    runtime: node
    plan: free
    buildCommand: NODE_ENV=development npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
